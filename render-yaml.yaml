services:
  - type: web
    name: espaco-vanessa-cubula
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: espacodb
          property: connectionString

databases:
  - name: espacodb
    plan: free